apiVersion: nais.io/v1alpha1
kind: Application
metadata:
  name: sosialhjelp-oppgaver-frontend
  namespace: teamdigisos
  labels:
    team: teamdigisos
spec:
  ingresses:
    - https://sosialhjelp-oppgaver.intern.dev.nav.no
  env:
    - name: BACKEND_AUDIENCE
      value: api://dev-gcp.teamdigisos.sosialhjelp-oppgaver-api/.default
